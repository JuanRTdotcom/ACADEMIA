<script lang="ts">import type { PageProps } from './$types'; import { Badge, Card, Icon, i18n } from '$lib'; let { data }: PageProps = $props();</script>
<Card padding="none"><div class="border-b border-hairline px-5 py-4"><h2 class="text-lg font-semibold text-ink">{i18n.t('pets.tabReminders')}</h2><p class="mt-1 text-sm text-steel">{i18n.t('pets.remindersHelp')}</p></div>{#if data.mascota.recordatorios.length}<div class="divide-y divide-hairline">{#each data.mascota.recordatorios as reminder}<article class="grid gap-3 px-5 py-4 sm:grid-cols-[44px_minmax(0,1fr)_auto] sm:items-center"><span class="grid size-10 place-items-center rounded-lg bg-primary-soft text-primary"><Icon name="bell-ring" size={18}/></span><div><div class="flex flex-wrap items-center gap-2"><strong class="text-sm text-ink">{reminder.titulo}</strong><Badge variant="outline-sky">{reminder.tipo.etiqueta}</Badge></div>{#if reminder.detalle}<p class="mt-1 text-sm text-steel">{reminder.detalle}</p>{/if}</div><div class="sm:text-right"><strong class="block text-sm text-ink">{new Intl.DateTimeFormat(i18n.locale,{dateStyle:'medium',timeStyle:'short',timeZone:data.usuario.preferencias.zona_horaria}).format(new Date(reminder.programado_para))}</strong><span class="text-xs text-stone">{reminder.estado_recordatorio.etiqueta}</span></div></article>{/each}</div>{:else}<div class="grid place-items-center px-5 py-14 text-center"><Icon name="bell-off" size={32} class="text-stone"/><p class="mt-3 text-sm text-steel">{i18n.t('pets.noReminders')}</p></div>{/if}</Card>
